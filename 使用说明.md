# LR1 Control 使用说明

这是 ILX-LR1 的 macOS 原生控制台，可调整相机允许修改的拍摄参数、查看实时取景、拍摄照片并传回 Mac，以及将取景画面录制为本地视频。照片和本地视频均可在相机不插存储卡的情况下使用。

## 首次连接：USB

1. 为 LR1 接好独立电源，装好镜头，用支持数据传输的 USB 线连接 Mac。首次设置相机菜单时可连接 HDMI 显示器。
2. 在相机中选择 `MENU → 设置 → USB → USB连接模式 → PC遥控（PC Remote）`；若设为“连接时选择”，插线后选择 `Remote Shoot (PC Remote)`。[相机 USB 连接说明](https://helpguide.sony.net/ilc/2390/v1/en/contents/0601_usb_connection.html)
3. 程序连接后会自动设置无卡照片直传。也可在相机菜单核对以下两项：
   - `MENU → 网络 → 连接/PC遥控 → 遥控拍摄设置（Remote Shoot Setting）→ 静止影像保存目的地 → 仅目的地（Destination Only）`。
   - `MENU → 拍摄 → 快门/静音 → 无存储卡释放快门（Release w/o Card）→ 允许（Enable）`。

   **不要选择“仅相机”或“目的地+相机”**：这两种保存目的地在无卡时仍会阻止快门释放。仅允许无卡释放快门，不等于照片已经保存到电脑。[照片保存目的地](https://helpguide.sony.net/ilc/2390/v1/en/contents/201h_remote_shoot_setting.html)、[无卡释放快门](https://helpguide.sony.net/ilc/2390/v1/en/contents/0414G_release_without_card.html)
4. 退出其他占用相机的遥控软件；相机也不要同时连接手机。在 Mac 打开 `LR1 Control.app`。如 macOS 询问是否允许 USB 配件连接，解锁 Mac 后点击“允许”。[Apple 配件连接说明](https://support.apple.com/en-gb/102282)
5. 点击“搜索”，选择 LR1，再点击“连接相机”。状态显示已连接后，取景画面和相机支持的参数会出现。

菜单译名可能随相机语言和固件有所不同，上述英文名称可用于核对。

## 拍摄与保存

- **照片**：按需要调整曝光模式、快门、光圈、ISO 等；不可写的参数只显示当前值。“AF”触发自动对焦，“拍摄照片”触发一张照片。照片文件经 SDK 直接保存到 Mac，格式由相机照片设置决定。等到界面显示下载完成和文件名后，再通过“显示照片”核对文件。
- **连拍**：在“连拍”区选择相机当前支持的档位，以及 0.5、1、2、3、5 或 10 秒的自动停止时间。点击“开始连拍”后，由相机原生连拍，程序到时释放快门，也可提前点击“停止”。时间控制的是快门保持时长，实际照片张数由相机决定。
- 连拍照片同样直接保存到 Mac，无需存储卡，每轮保存在所选文件夹下独立的“连拍_时间_编号”子文件夹中。停止拍摄后仍会继续接收文件；RAW＋JPEG 或 RAW＋HEIF 按同一文件编号配对，两个文件收齐才计为一张完整照片。等本轮回传完成后，再开始下一轮或单张拍摄。
- 连拍和回传期间，参数调整、切换保存目录及断开连接暂停使用，避免中断照片接收。程序会区分“正在连拍”和“等待照片传回”。
- **手动对焦**：在“手动对焦”区切换为 MF 后，选择细、中或粗步进，点击“近焦”或“远焦”移动镜头。也可输入目标焦位并点击“应用”；“当前焦位”显示相机实际回读值。可用功能与范围按当前镜头读取，焦位是归一化数值，不是以米表示的拍摄距离。
- 指定焦位期间可点击“停止调焦”；移动结束后再拍摄。近远微调是否清晰需结合实时取景判断。
- **视频**：先确认实时取景正常，再点击视频区的“开始”；点击“停止”后等待保存完成，通过“显示视频”查看。录制保持实时取景开启，生成 H.264 编码的 `.mov` 文件，无音轨。
- 视频记录的是 **SDK 提供的取景画面**，使用收到的取景分辨率及实际帧到达时间；不启动相机机内录像，也不需要存储卡。它不代表相机原生 4K/25p 视频，流畅度取决于取景帧率、传输和 Mac 的处理速度。
- 默认保存位置是 `~/Pictures/LR1 Captures`；可在“本地保存位置”点击“更改…”。照片和本地视频保存在该文件夹中。录制完成后再断开相机或退出程序。

## 网络连接（可选）

LR1 的有线网络通过 USB-LAN 转接器连接。将 Mac 与相机接入同一局域网，按相机的 `USB-LAN/Tethering` 菜单启用连接，并使两端 IP 地址位于同一子网；也可使用相机支持的 Wi-Fi 连接。[USB-LAN 设置](https://helpguide.sony.net/ilc/2390/v1/en/contents/221h_usb_lan_tether.html)

在相机的 `网络 → 连接/PC遥控 → PC Remote Function` 中开启 `PC Remote`。使用访问认证时，在 `网络 → 网络选项 → Access Authen. Info` 查阅相机用户名、密码和指纹，在程序“网络认证”中填写后连接；指纹必须与相机显示的值一致（区分大小写）；程序不保存密码。认证失败时先核对相机信息，再重试。如 macOS 提示本地网络权限，应允许本应用访问。[PC 遥控](https://helpguide.sony.net/ilc/2390/v1/en/contents/0903_pc_remote_function.html)、[访问认证设置](https://helpguide.sony.net/ilc/2390/v1/en/contents/221h_access_authentication_settings.html)、[认证信息](https://helpguide.sony.net/ilc/2390/v1/en/contents/221h_access_authentication_information.html)

## 常见问题

- **搜索不到相机**：检查相机供电、USB 数据线、USB 的 PC 遥控模式，以及 macOS 配件授权；关闭其他相机控制软件后重新搜索。网络连接再检查 IP 子网和本地网络权限。
- **能搜索到但连接超时（0x8208）**：退出可能访问相机的“预览”“图像捕捉”或其他遥控软件，再重新连接。本机曾由“预览”启动的相机服务占用 LR1，正常退出“预览”后连接恢复。
- **已连接但不能拍照**：检查上述无卡两项设置，退出相机回放状态，并确认当前为照片拍摄模式。自动对焦不能合焦时，检查镜头、对焦模式和画面。
- **已按快门但没有文件**：查看“运行日志”的错误，核对保存目录可写及磁盘空间；以本地文件完成下载为准。
- **视频按钮不可用或没有视频**：等待取景画面更新。没有收到有效取景帧时无法生成视频；先恢复取景，再重新开始录制。
- **参数不可修改**：可写参数和可选值由相机当前模式决定。例如自动曝光模式可能不允许手动改变快门或光圈。

## 从源码重新构建

需要 macOS 13 或更新版本、Xcode Command Line Tools，以及已下载的 Camera Remote SDK 2.02。当前脚本按本机架构编译；本机为 Apple silicon，SDK 动态库同时包含 arm64 与 x86_64。

在本源码文件夹执行：

```sh
bash Scripts/build.sh
```

默认 SDK 目录是 `~/Downloads/CrSDK_v2.02.00_20260610a_Mac`。如目录不同，可通过 `SDK_PATH` 指定含 `SimpleCli.zip` 的 SDK 文件夹。生成的应用默认位于源码文件夹的上一层。

应用打包时保留 SDK 库的目录关系：

```text
LR1 Control.app/Contents/
  MacOS/LR1Control
  Frameworks/libCr_Core.dylib
  Frameworks/CrAdapter/libCr_PTP_IP.dylib
  Frameworks/CrAdapter/libCr_PTP_USB.dylib
  Frameworks/CrAdapter/libusb-1.0.0.dylib
  Frameworks/CrAdapter/libssh2.dylib
```

主程序的运行时搜索路径为 `@executable_path/../Frameworks`，Core 的加载名为 `@rpath/libCr_Core.dylib`。适配器通过动态加载使用，不能只凭主库的 `otool -L` 结果删除 `CrAdapter`。普通取景使用 `GetLiveViewImage` 返回的 JPEG 帧；SDK 的独立 monitoring 库用于其他监看/回放功能，构建脚本目前一并保留。

构建脚本先对内嵌动态库签名，再对应用做本地 ad-hoc 签名并验证；修改二进制、加载路径或架构后应重新签名。向其他 Mac 正式分发时，应使用 Developer ID 签名并完成公证。应用中的 SDK 库来自本地官方 SDK 包，不依赖 Homebrew 库。

2026-09-22 已用本机 LR1 完成 USB 无卡实测：Mid 1 秒自动停止，以及提前手动停止各得到 3 张 RAW＋HEIF，共 12 个文件完整传回；6 个 HEIF 均完整解码为 9504×6336。连拍后单张照片仍能传回默认目录。手动对焦已验证近焦、远焦微调与指定焦位的实际位置回读。

## 离线测试

执行 `bash Scripts/test-local-video.sh` 可验证本地 MOV 的编码、帧数、时长、画面方向与错误回调。测试使用合成画面，不操作相机。

执行 `bash Scripts/test-camera-bridge.sh` 可离线验证连拍回传分组、定时停止、手动对焦与连接失败清理；`SDK_PATH` 与构建脚本相同，测试使用独立临时目录，不连接相机、不写应用日志。
